
const ShowButton = () => {
  /*  let greeting = "Hola";
    let farewell = "Adios";
    const change () =>{
        x == false ? greeting : farewell
    };*/

    return (
        <div>
            <h3>{change}</h3>
            <button onClick={() => x ==false }>Click</button>
        </div>
    );
};

export default ShowButton;